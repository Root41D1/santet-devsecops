title = "Repository secret-scanning policy"

[extend]
useDefault = true

# Add narrowly scoped allowlists only for verified test fixtures. Every entry
# should reference an approved, expiring security exception.
[allowlist]
description = "No repository-wide allowlist entries"
paths = []

